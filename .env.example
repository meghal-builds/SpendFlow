# SpendFlow Environment Variables Example
# SpendFlow is an offline-first app (SQLite local database) so no API keys are required by default.
# If you integrate external services in the future, add variables prefixed with EXPO_PUBLIC_:

# EXPO_PUBLIC_API_URL=https://api.example.com
# EXPO_PUBLIC_ANALYTICS_KEY=your_key_here
